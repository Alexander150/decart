<body ng-controller="CurrencyCtrl" ng-init="init(<%= params[:id] %>);">
	<header class="header" style="width: 50vw;">
		<div>
			<h1 style="text-align: center;">Все данные о валюте "{{ currency.name }}"</h1>
		</div>
	</header>
	<b>Время обновления информации: {{ time.getHours() }}:{{ time.getMinutes() }}</b>

	<table align="center" width="80%">
		<tr>
			<th>Id</th>
			<th>Название валюты</th>
			<th>Курс валюты к рублю</th>
		</tr>
		<tr>
			<td>{{ currency.id }}</td>
			<td>{{ currency.name }}</td>
			<td>{{ currency.rate }}</td>
		</tr>

	</table>

	<a href="/all" style="text-decoration: none; color: black; position: fixed; left: 25px; bottom: 25px;"><b>Все валюты</b></a>
	<a href="/" style="text-decoration: none; color: black; position: fixed; right: 25px; bottom: 25px;"><b>Стартовая страница</b></a>
</body>